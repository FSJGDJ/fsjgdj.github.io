# 2026-09-20

- 为个人博客（fsjgdj.github.io，Astro 搭建）输出界面美化方案文档 `docs/blog-redesign-plan.md`。
- 用户选定方向：阶段一精修补硬伤（代码块样式+复制按钮、手动明暗切换、头像/个人标识、标签+阅读时长、文章目录、页脚+RSS），阶段二向"杂志感极简"靠拢（去卡片化、衬线标题、配色收敛）。
- 用户明确要求：先出方案文档，不要直接改代码。方案已交付，等待确认后再实施。

## 实施记录（下午）

按 `docs/blog-redesign-plan.md` 两阶段全部实施完毕，用户要求：个人标识（头像/昵称/介绍/邮箱）自己填、不提交 git。

阶段一（补硬伤）：
- `src/layouts/Layout.astro`：重写。明暗切换（data-theme + localStorage + head 内联防 FOUC 脚本）、页脚一行式（GitHub/RSS/Astro，邮箱留 TODO 注释）、代码块深色样式 + 复制按钮（JS 包 wrapper 方案，避免 overflow 滚动按钮跑掉）
- `src/config.ts`：新增站点个人信息配置，用户改这一个文件即可填昵称/定位/头像/邮箱
- `public/avatar.svg`：占位头像（渐变+人形剪影）
- `src/pages/index.astro`：Hero 加头像+昵称+定位+社交图标（GitHub/RSS/邮箱 SVG）；卡片加标签胶囊和阅读时长（中文 400 字/分钟估算）
- `src/pages/blog/[slug].astro`：TOC（窄屏 details 折叠、>1100px 右侧 fixed sticky，IntersectionObserver 高亮当前章节）、标签、阅读时长、表格样式、pre 内 code 不吃行内样式
- `src/content.config.ts`：schema 加 `tags: z.array(z.string()).default([])`
- 四篇 md 补了 tags；新增 `src/pages/rss.xml.js`（@astrojs/rss）；astro.config.mjs 加 site

阶段二（视觉减法）：
- 配色收敛：accent #6366f1→#4f46e5（暗 #a5b4fc），bg #f7f7f8→#fafafa（暗 #111114）
- 容器 720→680px；h1-h3 衬线字体（--font-serif 栈，无下载字体）
- 首页列表去卡片化：细分割线 + 悬停标题变色+左竖线；Hero 渐变标题改纯色
- 文章页 .content 去卡片容器；字号比例 h2 1.5rem/h3 1.25rem，行高 1.75

未做（方案中标记"可选"）：View Transitions——与防 FOUC 主题脚本配合需处理 astro:after-swap 事件，为稳妥跳过。

验证：`astro build` 通过（5 页 + rss.xml）；dev server localhost:4321 三页 HTTP 200。

## 追加：站点目录页（12:30）

- 新增 `src/pages/archive.astro`：全站文章目录页，按年份分组，日期（等宽字体）+ 标题列表，悬停样式与首页一致（标题变色 + 左竖线）
- `Layout.astro` 导航加"目录"入口（首页 / 目录 / GitHub）
- 注意：文章页的章节 TOC 与站点目录页是两回事，用户说"加目录"时需确认指哪个
- 构建通过（6 页），dev server /archive/ 200

## 追加：全屏背景图 + 毛玻璃（12:40）

- 用户提供照片 `D:\picture\elg21-fog-4436636_1920.jpg`（云海山峦，314KB）→ 复制为 `public/images/bg.jpg` 作全站背景
- 参考 diary.fulitower.art 的照片背景+半透明质感；用户明确排除：鼠标拖拽动画、花瓣特效
- 实现：`body::before` fixed cover 背景图 + `body::after` 可读性遮罩（亮白纱 0.45/暗深纱 0.55）；`--card` 改半透明（亮 rgba(255,255,255,.72)/暗 rgba(23,26,33,.74)）；main 内加 `.glass-panel`（blur16+圆角16+投影）包住全部内容；header blur14、footer 半透明底；文章页 `.toc-desktop` 独立毛玻璃底
- 改动已补进 `docs/blog-redesign-plan.md` 第七节（阶段三），含验收标准
- 构建通过（6 页）

## 追加：首页加宽 + 间距 + 进场动画（12:51）

- 参考 diary.fulitower.art 的 fadeInUp 动画与大间距（抓 HTML 分析：@keyframes fadeInUp translateY(200%)→0，homepage-load-animation 2s）
- Layout 加 `wide` prop → `.container-wide` 780px（仅首页）；文章页保持 680px
- 首页条目上下内边距 1.5→2rem，悬停竖线改全高（top/bottom 2rem）
- 进场动画：hero+每条 `.reveal`，IO 触发 `.visible`，上浮 26px+淡入 0.7s，逐条延迟 90ms（最多 8 条）；html.js 守卫防无 JS 白屏；prefers-reduced-motion 禁用
- 方案文档补第八节。构建通过。

## 追加：首页改独立卡片（12:55）

- 用户发参考站截图：两篇文章之间要有露出背景的空隙 → 每篇文章独立毛玻璃卡片，不是连续列表
- Layout 加 `panel` prop（默认 true），首页 `panel={false}` 去掉整张面板，Hero 和卡片直接浮在照片上
- `.post-card` 改玻璃卡片样式（blur+圆角14+投影），列表 gap 1.6rem，悬停改卡片上浮+边框染色，移除左竖线
- 目录页/文章页保持整版面板。方案文档第八节加了"修订"小节。构建通过。

## 追加：首屏 Landing（13:00）

- 用户要求：进站先显示整屏 Landing（照片+居中头像/站名/标语+底部下滑箭头），滚动后才见文章
- index.astro：hero 改 `.landing`（min-height 100svh-10rem 居中，头像 96px），底部 `.scroll-hint` 下箭头（bounce 2.2s 循环），文章移入 `#posts` 区（scroll-margin-top 5rem）
- config.ts 加 `motto` 字段（衬线大字标语位，留空不显示，留给用户填）
- Layout 全局 scroll-behavior: smooth（reduced-motion 降级）
- 方案文档补第九节。构建通过。

## 追加：Logo 改名左移 + 去卡片阅读时长（13:09）

- 导航栏 Logo：🚀 我的博客 → 🚀 fsjdg的空间；header-inner 去掉 .container 改全宽 padding 1.5rem，Logo 移到页面最左、导航最右
- 首页卡片 meta 去掉"约 X 分钟"（readingTime 函数一并移除）；文章页仍保留阅读时长
- 构建通过。注意：页脚/标题/RSS 里仍是"我的博客"，用户未要求改，后续可能提

## 追加：填写个人标识（13:15）

- 头像：用户 Desktop 的 mmexport1789880962426.jpg（星空动漫图，61KB）→ `public/images/avatar.jpg`；占位 avatar.svg 已删
- config.ts：name=FSJDG，tagline=道阻且长，行则将至。motto 仍留空
- 构建通过，首页已验证生效




## 补充（13:17）

- 首页社交图标：移除 RSS 图标（`/rss.xml` 订阅源保留可用），改为邮箱图标位——`siteConfig.email` 填了才显示，邮箱由用户自填（`src/config.ts`）
- 头像/昵称/介绍已由用户填入：avatar.jpg、FSJDG、"道阻且长，行则将至。"
- 用户填入邮箱 fsjdg2025@163.com：`src/config.ts` email 字段；`Layout.astro` 页脚邮箱 TODO 改为读 siteConfig（有邮箱才显示）。首页社交图标现显示 GitHub + 邮箱。
- 用户已自行提交 git（改动后 origin/main 已同步）。移除导航栏 Logo 前的 🚀 emoji，改为纯文字"fsjdg的空间"。
