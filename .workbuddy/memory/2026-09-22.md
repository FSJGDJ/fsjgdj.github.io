# 2026-09-22

- 阅读桌面上的《fsjgdj美化方案v2.md》，与站点现状逐项比对，输出差距分析 + 处理计划（未改任何代码，等用户拍板）。结论：16 项中 3 项已实现（暗黑模式、淡入动画、TOC）、5 项部分实现（CSS 变量缺 --radius/--transition、正文 16px 无 h2 下边框、导航缺 active 指示、卡片缺封面图/阅读时长、归档缺标签维度）、5 项完全缺失（全文搜索、/tags、/about、页脚统计+运行时间、阅读进度条）、3 项与现状冲突（配色/标题字体/首屏布局）需用户决策。
- 关键判断：方案要求的纯色卡片 + 无衬线标题 + 左右分栏 Hero，与站点既有风格（毛玻璃 + 背景图 + 衬线标题）和用户上轮明确保留的偏好相冲突，不能照搬；方案里的 --color-* 变量命名与现有 --bg/--text 体系并存会造成混乱，建议只补缺失变量、不换命名。

- 首页改分栏：滚过首屏后 左文章列表 + 右站点统计面板（毛玻璃卡片，sticky top 4.6rem 粘性固定，ttdr 同款效果）；统计为 文章数/标签数/运行天数（自最早文章起算；无分类体系，故未显示分类数，如需真分类要加 frontmatter 字段）。container-wide 780→1120px（仅首页用）。窄屏(<1024px)回退单栏、统计变横向条不粘性。浏览器实测 1440：两列 814+280，滚动后列表 x 恒定无位移、侧栏钉在视口 62px。配色/毛玻璃/背景图/衬线标题均未动。

- 三功能上线（均已浏览器实测）：①首页卡片补 字数·阅读时长（新工具 src/utils/postStats.ts，剔代码块后 CJK按字+其余按词，400字/分；[slug].astro 阅读时长也改用同函数统一口径）；②正文图片 Lightbox（[slug].astro 脚本运行时创建遮罩挂 body 避开 backdrop-filter 劫持；ESC/遮罩/按钮关闭，方向键+箭头切换，touch 滑动阈值40px，样式全部 :global）；③左侧作者名片替换右侧站点概览（头像+FSJDG+渴望成为大手子，sticky 钉住，窄屏变横条）
- 教训1：grid 用 align-items:start 时，sticky 必须放在 grid item（aside）本身——其包含块是整行高的 grid area；放在内部卡片上时 aside 只有内容高度，sticky 无滑动空间失效
- 教训2：同一文件不要在一条消息里发多个并行 Edit——本次第一个编辑被第二个静默覆盖导致 sticky 修复丢失，同文件编辑必须串行
- 注意：当前四篇文章正文均无图片，Lightbox 用注入测试图验证；用户加图后自然生效

- 侧栏增强（按计划v2执行）：index.astro 左栏改为双卡——①作者名片（站名fsjdg的空间+头像+FSJDG+渴望成为大手子+motto衬线+统计行4文章/4标签/N天+GitHub/邮箱/RSS圆钮Links）②图标导航卡（首页/归档/GitHub，当前页accent高亮+左竖线，Astro.url.pathname判断）。配色只用现有变量零新增。窄屏(<1024px)：导航卡隐藏、名片横条、motto/links隐藏、统计行整行显示。sticky沿用aside方案（双卡整体钉住）。实测activeColor=rgb(79,70,229)=#4f46e5✓ 无重叠✓ sticky✓ 窄屏回退✓

- 侧栏微调：作者名片删除站名行「fsjdg的空间」（.author-site 模板+样式一并清理，.author-name 补上 0.9rem 顶距保持头像间距），motto 由 siteConfig.tagline 改为固定文案「学习记录和个人兴趣」；导航卡删除 GitHub 项（与顶部导航重复），仅剩 首页/归档。实测头像->昵称间距 23px 正常、无错位
